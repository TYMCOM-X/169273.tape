.library man.dfs
.synopsis
Trim leading and trailing blanks off lines.
.syntax
TRIM infiles /options
TRIM outfile = infiles /options
.description
The input files are copied to the output file with the leading
and trailing blanks and tabs stripped from each line.

If the /LEADING option is specified,
only leading blanks are stripped.
If the /TRAILING option is specified,
only the trailing blanks are stripped.
The default is both.
Both options may be abbreviated to the first character.
