.calliDef -14,REDNXT,|peek at input|;
.b!ex
	REDNXT	ac,
	buffer-empty return
	character-present return
.e!ex
If the command port's input buffer is empty,
nonskip-return.

Otherwise,
copy one 7-bit character from the input buffer to
<ac>, right-justified with leading bits zero
(without removing that character from the buffer)
and skip-return.
.endSec REDNXT:
