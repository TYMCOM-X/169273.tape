The KERMIT files for the DECsystem-10 have been renamed to K10*.*.
After moving these files to a DEC-10, replace the "K10" prefix with "KER",
so that K10MIT.MAC becomes KERMIT.MAC and so forth, and then the names
will jibe with the .CCL files and the documentation.
