#system(DTIME - day/time manipulation routines)
#module(CV_BIN_STR,11/01/78)
Converts an integer to a string.
#usage
EXTERNAL FUNCTION CV_BIN_STR(INT_VAL: INT): INT_STR;
#input(INT_VAL)an arbitrary integer.
#output(<return value>)a string representation of the absolute
value of INT_VAL.  The string is zero
suppressed except for 0, which is returned as '0'.
#include(DTIME.TYP)
#end
